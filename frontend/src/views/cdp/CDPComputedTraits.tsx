/**
 * CDP Computed Traits - Derived profile attribute builder
 */

import { ComputedTraitBuilder } from '@/components/cdp/ComputedTraitBuilder';

export default function CDPComputedTraits() {
  return <ComputedTraitBuilder />;
}
