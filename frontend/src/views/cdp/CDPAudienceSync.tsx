/**
 * CDP Audience Sync - Push CDP segments to Meta (Facebook, Instagram & WhatsApp)
 */

import AudienceSync from '@/components/cdp/AudienceSync';

export default function CDPAudienceSync() {
  return <AudienceSync />;
}
